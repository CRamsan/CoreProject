ext.libraryVersionCode = 1
ext.libraryVersionName = "0.1"
disableJsTarget()

apply from: "$rootDir/gradle/kotlin-mpp-lib.gradle"

/**
 * Configure multiplatform project
 */
kotlin {
    sourceSets {
        commonMain {
            dependencies {
                implementation project(":framework")
            }
        }
        commonTest {
            dependencies {
                implementation project(":framework:test")
            }
        }
        jvmMain {
            dependencies {
            }
        }
        jvmTest {
            dependencies {
                implementation KotlinX.coroutines.swing
            }
        }
    }
}

registerDependency(":framework")

android {
    namespace 'com.cramsan.doom.lib'
}