apply from: "$rootDir/gradle/kotlin-mpp-lib.gradle"

kotlin {
    js(IR) {
        browser()
        nodejs()
        binaries.executable()
    }
}

android {
    namespace 'com.cramsan.framework.interfacelib'
}