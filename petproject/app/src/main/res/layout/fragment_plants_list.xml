<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.cramsan.petproject.plantslist.PlantListViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout style="@style/BaseContentLayout">

        <FrameLayout
            android:id="@+id/frameLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ProgressBar
                style="?android:attr/progressBarStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/plants_list_loading"
                android:layout_gravity="center"
                app:observableVisibility="@{viewModel.observableLoadingVisibility()}" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/plant_list_recycler"
                app:layoutManager="LinearLayoutManager"
                app:fastScrollEnabled="true"
                app:fastScrollHorizontalThumbDrawable="@drawable/thumb_drawable"
                app:fastScrollHorizontalTrackDrawable="@drawable/line_drawable"
                app:fastScrollVerticalThumbDrawable="@drawable/thumb_drawable"
                app:fastScrollVerticalTrackDrawable="@drawable/line_drawable"
                app:observableVisibility="@{viewModel.observablePlantListVisibility()}"
                style="@style/BaseContentLayout"
                tools:listitem="@layout/view_plant"
                tools:context=".plantslist.PlantsListFragment"></androidx.recyclerview.widget.RecyclerView>

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/plant_list_add_plant"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end|bottom"
                android:clickable="true"
                android:focusable="true"
                android:layout_margin="16dp"
                app:srcCompat="@drawable/ic_plus" />
        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>